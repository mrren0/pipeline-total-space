apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: total-site
  namespace: dev # В пайплайне подставится dev/prod
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: dev.total-space.online # В пайплайне подставится dev/total-space
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: total-site
                port:
                  number: 80
